:set -fno-ghci-sandbox
:{
:def initR
     (\_ -> return
        $    ":m + H.Prelude"
        ++ "\n:def initR_ \\_ -> initializeR Nothing "
        ++                   ">> (writeIORef Language.R.globalEnv =<< peek Foreign.R.globalEnv) "
        ++                   ">> return \"\""
        ++ "\n:initR_"
        ++ "\n:undef initR_"
     )
:}
